{
  "name": "rider-pc-web",
  "version": "1.0.0",
  "private": true,
  "description": "CSS/HTML linting and tools for Rider-PC dashboard",
  "scripts": {
    "lint:css": "stylelint 'web/assets/**/*.css'",
    "lint:css:fix": "stylelint 'web/assets/**/*.css' --fix",
    "css:size": "node scripts/check-css-size.js"
  },
  "devDependencies": {
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
