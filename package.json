{
  "name": "rider-pc-web",
  "version": "1.0.0",
  "private": true,
  "description": "CSS/HTML linting and tools for Rider-PC dashboard",
  "scripts": {
    "lint:css": "npx stylelint 'web/assets/**/*.css'",
    "lint:css:fix": "npx stylelint 'web/assets/**/*.css' --fix",
    "css:size": "node scripts/check-css-size.js",
    "css:audit": ".venv/bin/python scripts/css_audit.py"
  },
  "devDependencies": {
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
