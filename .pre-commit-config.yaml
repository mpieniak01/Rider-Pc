repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        name: ruff (lint + fix)
        args: ["--fix", "--unsafe-fixes"]
      - id: ruff-format
        name: ruff (format)
